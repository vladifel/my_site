import {
    createMuiTheme
} from '@material-ui/core/styles';

export default createMuiTheme({
    root: {
        height: '100%',
        width: '100%',
    },
    typography: {
        fontFamily: ['BioRhyme'],
        //textAlign: 'left',
        //fontSize: '20px'
    },
});